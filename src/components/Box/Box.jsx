import styled from 'styled-components';
import {
  color,
  space,
  layout,
  flexbox,
  border,
  typography,
  grid,
} from 'styled-system';

export const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  border,
  typography,
  grid
);
